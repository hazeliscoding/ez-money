# Copy to .env and adjust if needed. Defaults match docker-compose.yml.
PORT=3000

DB_HOST=localhost
DB_PORT=5433
DB_USER=ezmoney
DB_PASSWORD=ezmoney
DB_NAME=ezmoney

# Python interpreter used to run the ezmoney PDF parser (must have deps installed).
PYTHON_BIN=python
